# Required — set this in Vercel Dashboard > Project > Settings > Environment Variables
# NEVER use the VITE_ prefix — that would expose the key in the browser bundle
OPENAI_API_KEY=your_openai_api_key_here

